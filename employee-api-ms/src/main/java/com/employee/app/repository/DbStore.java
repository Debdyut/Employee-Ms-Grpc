package com.employee.app.repository;

//@Component
public class DbStore {

//	private static final Map<String, EmployeeBase> employeeEntities = new ConcurrentHashMap<>();
//
//	public DbStore() {
//		// Seed Employee for testing
//		EmployeeBase employee = EmployeeBase.newBuilder().setAge(21)
//				.setDob(Date.newBuilder().setDay(20).setMonth(8).setYear(1990).build()).setName("Bruce Willis")
//				.setSalary(11122244.40).build();
//		employeeEntities.put(employee.getName(), employee);
//	}
//
//	public EmployeeRequest.Response createEmployee(EmployeeRequest req) {
//		// validate request object
//		// Owner and receiver should be taken from req. in the form of ID
//
//		for (EmployeeBase item : req.getEmployeeList()) {
//			EmployeeBase employee = EmployeeBase.newBuilder().setAge(item.getAge())
//					.setDob(Date.newBuilder().setDay(20).setMonth(8).setYear(1990).build()).setName(item.getName())
//					.setSalary(11122244.40).build();
//			employeeEntities.put(employee.getName(), employee);
//		}
//
//		return EmployeeRequest.Response.newBuilder().setId(UUID.randomUUID().toString()).build();
//	}

}
